#include <stdio.h>
/**
 * main - A ptogram that print a line using the printf function
 * Return: 0
 */
int main(void)
{
	Printf("with proper grammar, but the outcome is a piece of art,\n")
      return (0);
}
